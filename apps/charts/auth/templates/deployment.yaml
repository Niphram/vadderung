apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
  annotations:
    reloader.stakater.com/auto: "true"
spec:
  selector:
    matchLabels:
      app: authelia-app
  replicas: 2
  strategy:
    type: RollingUpdate
  revisionHistoryLimit: 2
  template:
    metadata:
      labels:
        app: authelia-app
    spec:
      enableServiceLinks: false
      containers:
        - name: {{ .Release.Name }}
          image: {{ .Values.authelia.image }}
          volumeMounts:
            - name: config-volume
              mountPath: /config
              readOnly: true
            - name: providers-volume
              mountPath: /run/secrets/providers
              readOnly: true
            - name: authelia-secret
              readOnly: true
              mountPath: "/run/secrets/authelia"
            - name: authelia-oidc-secret
              readOnly: true
              mountPath: "/run/secrets/oidc"
            - name: authelia-jwk-rsa
              readOnly: true
              mountPath: "/run/secrets/jwks"
            - name: authelia-users-secret
              readOnly: true
              mountPath: "/etc/users"
            - name: db-secret
              readOnly: true
              mountPath: "/etc/db-secret"
            - name: data-volume
              mountPath: /data
              readOnly: false
          env:
            - name: X_AUTHELIA_CONFIG_FILTERS
              value: template
            - name: AUTHELIA_LOG_LEVEL
              value: trace
            - name: TZ
              value: {{ .Values.authelia.timezone }}
      volumes:
        - name: config-volume
          configMap:
            name: "{{ .Release.Name }}-cm"
        - name: providers-volume
          configMap:
            name: authelia-oidc-providers-cm
        - name: authelia-secret
          secret:
            secretName: authelia-secret
        - name: authelia-users-secret
          secret:
            secretName: authelia-users-secret
        - name: authelia-oidc-secret
          secret:
            secretName: authelia-oidc-secret
        - name: authelia-jwk-rsa
          secret:
            secretName: authelia-jwk-rsa
        - name: db-secret
          secret:
            secretName: {{ .Values.authelia.pgsecrets }}
        - name: data-volume
          emptyDir:
            sizeLimit: 100Mi
---
kind: Service
apiVersion: v1
metadata:
  name: authelia-service
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: authelia-app
  ports:
    - protocol: TCP
      targetPort: 9091
      port: 80
