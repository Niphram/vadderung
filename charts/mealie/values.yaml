mealie:
  replicas: 1 # Mealie doesn't play well with multiple pods
  pgsecrets: mealie-data-app

postgres:
  instances: 3
  superuser: false # Enables superuser rights for the app user (needed to restore mealie backups)