apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Chart.Name }}-service
subsets:
- addresses:
  - ip: {{ .Values.valetudo.robot.ip }}
  ports:
  - name: http
    port: {{ .Values.valetudo.robot.port }}
    protocol: TCP