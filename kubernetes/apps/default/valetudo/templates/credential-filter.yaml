apiVersion: gateway.envoyproxy.io/v1alpha1
kind: HTTPRouteFilter
metadata:
  name: valetudo-credential-injection
spec:
  credentialInjection:
    overwrite: true
    credential:
      valueRef:
        name: valetudo-credential