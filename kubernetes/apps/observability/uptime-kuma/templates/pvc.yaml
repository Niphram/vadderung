apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: uptime-kuma-data-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-2
  resources:
    requests:
      storage: 4Gi
